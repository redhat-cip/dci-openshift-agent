---
- hosts: registry_host
  become: true

  tasks:
  - name: "infrastructure : Setup podman"
    tags:
    - podman
    include_role:
      name: podman-setup

  - name: "infrastructure : Create local registry"
    tags:
    - registry
    include_role:
      name: local-registry-setup
...
