apiVersion: trident.netapp.io/v1
kind: TridentProvisioner
metadata:
  name: trident
  namespace: trident
spec:
  debug: true
{% if (registryHost is defined) %}
  imageRegistry: "{{ registryHost }}"
{% endif %}
