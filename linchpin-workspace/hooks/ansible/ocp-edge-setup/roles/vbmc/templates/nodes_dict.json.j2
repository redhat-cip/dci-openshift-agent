{ {% for host_string in nodes_info.results|map(attribute='stdout')|list %}{% set host_data = host_string|from_yaml %}"{{ host_data.name.split('-')[0] }}-{{ host_data.name.split('-')[1] }}": {"ipmi_user": "{{ vbmc_user }}", "ipmi_pass": "{{ vbmc_pass  }}", "ipmi_address": "{{ vbmc_management_address }}:{{ vbmc_ports[host_data.name] }}","mac_address": "{{ host_data.mac }}"}{% if not loop.last %},{% endif %}{% endfor %} }
