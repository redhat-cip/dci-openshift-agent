- name: Configure vbmc
  hosts: localhost
  any_errors_fatal: true
  become: true
  tasks:
      - include_role:
            name: vbmc
        vars:
            vbmc_host: localhost
            vbmc_nodes: "{{ groups['openshift'][:-1] }}"
