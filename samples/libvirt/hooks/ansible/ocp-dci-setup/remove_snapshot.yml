- name: Snapshot Cleanup
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Remove provisionhost Snapshot
      become: true
      ignore_errors: true
      command: |
          virsh snapshot-delete --domain {{ groups['worker'][-1] }} \
                                --name {{ groups['worker'][-1] }}-before-provision
