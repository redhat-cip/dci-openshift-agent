FROM registry.redhat.io/ubi10/ubi:latest

# Install lshw and other useful tools
RUN dnf update -y && \
    dnf install -y lshw && \
    dnf clean all

# Set working directory
WORKDIR /workspace

# Ensure container runs as root
USER root

# No default command - let the caller specify what to run
